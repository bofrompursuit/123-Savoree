// Keep in sync with next.config.ts's basePath. Only needed for raw <video>/
// <audio> src attributes — next/image and next/link apply basePath
// automatically, but plain HTML elements don't.
export const BASE_PATH = "/123-Savoree";
