// Vercel serves the app at the domain root, so raw <video>/<audio> src
// attributes need no prefix (next/image and next/link never needed this
// either). Was "/123-Savoree" back when this deployed to GitHub Pages.
export const BASE_PATH = "";
